import { EyeDropperDirective } from './eye-dropper.directive';

describe('EyeDropperDirective', () => {
  it('should create an instance', () => {
    const directive = new EyeDropperDirective();
    expect(directive).toBeTruthy();
  });
});
