import { IntersectionObserverTargetDirective } from './intersection-observer-target.directive';

describe('IntersectionObserverTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new IntersectionObserverTargetDirective();
    expect(directive).toBeTruthy();
  });
});
