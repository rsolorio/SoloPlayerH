import { FilterCriteriaEntity } from './filter-criteria.entity';

describe('FilterCriteriaEntity', () => {
  it('should create an instance', () => {
    expect(new FilterCriteriaEntity()).toBeTruthy();
  });
});
