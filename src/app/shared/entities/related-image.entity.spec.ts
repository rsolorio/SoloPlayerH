import { RelatedImageEntity } from './related-image.entity';

describe('RelatedImageEntity', () => {
  it('should create an instance', () => {
    expect(new RelatedImageEntity()).toBeTruthy();
  });
});
