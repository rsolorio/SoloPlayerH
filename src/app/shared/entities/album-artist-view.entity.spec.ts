import { AlbumArtistViewEntity } from './album-artist-view.entity';

describe('AlbumArtistViewEntity', () => {
  it('should create an instance', () => {
    expect(new AlbumArtistViewEntity()).toBeTruthy();
  });
});
