import { PlayHistoryEntity } from './play-history.entity';

describe('PlayHistoryEntity', () => {
  it('should create an instance', () => {
    expect(new PlayHistoryEntity()).toBeTruthy();
  });
});
