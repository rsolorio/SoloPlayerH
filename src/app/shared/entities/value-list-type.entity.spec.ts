import { ValueListTypeEntity } from './value-list-type.entity';

describe('ValueListTypeEntity', () => {
  it('should create an instance', () => {
    expect(new ValueListTypeEntity()).toBeTruthy();
  });
});
