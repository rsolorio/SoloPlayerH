import { SongClassificationViewEntity } from './song-classification-view.entity';

describe('SongClassificationViewEntity', () => {
  it('should create an instance', () => {
    expect(new SongClassificationViewEntity()).toBeTruthy();
  });
});
