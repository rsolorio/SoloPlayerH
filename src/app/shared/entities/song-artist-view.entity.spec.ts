import { SongArtistViewEntity } from './song-artist-view.entity';

describe('SongArtistViewEntity', () => {
  it('should create an instance', () => {
    expect(new SongArtistViewEntity()).toBeTruthy();
  });
});
