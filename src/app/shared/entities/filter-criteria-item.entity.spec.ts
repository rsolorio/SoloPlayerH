import { FilterCriteriaItemEntity } from './filter-criteria-item.entity';

describe('FilterCriteriaItemEntity', () => {
  it('should create an instance', () => {
    expect(new FilterCriteriaItemEntity()).toBeTruthy();
  });
});
