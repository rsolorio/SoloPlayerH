import { PlaylistSongEntity } from './playlist-song.entity';

describe('PlaylistSongEntity', () => {
  it('should create an instance', () => {
    expect(new PlaylistSongEntity()).toBeTruthy();
  });
});
