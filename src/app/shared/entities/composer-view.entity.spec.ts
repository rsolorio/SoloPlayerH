import { ComposerViewEntity } from './composer-view.entity';

describe('ComposerViewEntity', () => {
  it('should create an instance', () => {
    expect(new ComposerViewEntity()).toBeTruthy();
  });
});
