import { PartyRelationEntity } from './party-relation.entity';

describe('PartyRelationEntity', () => {
  it('should create an instance', () => {
    expect(new PartyRelationEntity()).toBeTruthy();
  });
});
