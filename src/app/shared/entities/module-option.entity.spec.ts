import { ModuleOptionEntity } from './module-option.entity';

describe('ModuleOptionEntity', () => {
  it('should create an instance', () => {
    expect(new ModuleOptionEntity()).toBeTruthy();
  });
});
