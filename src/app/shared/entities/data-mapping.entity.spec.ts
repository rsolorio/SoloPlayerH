import { DataMappingEntity } from './data-mapping.entity';

describe('DataMappingEntity', () => {
  it('should create an instance', () => {
    expect(new DataMappingEntity()).toBeTruthy();
  });
});
