import { ValueListEntryEntity } from './value-list-entry.entity';

describe('ValueListEntryEntity', () => {
  it('should create an instance', () => {
    expect(new ValueListEntryEntity()).toBeTruthy();
  });
});
