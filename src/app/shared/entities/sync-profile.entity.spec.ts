import { SyncProfileEntity } from './sync-profile.entity';

describe('SyncProfileEntity', () => {
  it('should create an instance', () => {
    expect(new SyncProfileEntity()).toBeTruthy();
  });
});
