export enum BreadcrumbSource {
  AlbumArtist,
  Artist,
  Album,
  Classification,
  Genre
}

export enum BreadcrumbEventType {
  Add,
  AddMultiple,
  Remove,
  RemoveMultiple,
  Replace,
  Updated
}