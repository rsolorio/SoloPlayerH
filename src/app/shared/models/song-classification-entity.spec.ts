import { SongClassificationEntity } from './song-classification.entity';

describe('SongClassificationEntity', () => {
  it('should create an instance', () => {
    expect(new SongClassificationEntity()).toBeTruthy();
  });
});
