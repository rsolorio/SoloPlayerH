import { ClassificationEntity } from './classification.entity';

describe('ClassificationEntity', () => {
  it('should create an instance', () => {
    expect(new ClassificationEntity()).toBeTruthy();
  });
});
